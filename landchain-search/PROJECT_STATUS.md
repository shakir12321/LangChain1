# Project Goal
Create a new `landchain-search` project with dependency management via `pyproject.toml`.

# Current Status
- Project directory created.
- `pyproject.toml` added with requested dependencies.
- Basic `README.md` scaffolded with setup steps.

# Pending Tasks
- [x] Create project folder
- [x] Add `pyproject.toml` with dependencies
- [x] Add initial `README.md`
- [ ] Add starter application code (`main.py`)
- [ ] Add usage examples and environment variable docs
